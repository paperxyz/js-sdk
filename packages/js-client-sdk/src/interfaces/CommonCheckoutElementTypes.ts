export interface ICustomizationOptions {
  colorPrimary?: string;
  colorBackground?: string;
  colorText?: string;
  borderRadius?: number;
  fontFamily?: string;
}

export enum Locale {
  EN = 'en',
  FR = 'fr',
}
